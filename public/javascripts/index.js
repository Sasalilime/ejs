window.addEventListener('DOMContentLoaded', (event) =>{
    // const button = document.querySelector('button');
    // button.addEventListener('click', () => {
    //     console.log('test');
    // })


})


const greating = (message) => {
    console.log(message + ' bg');
}